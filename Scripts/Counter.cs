using UnityEngine;
using UnityEngine.UI;

public class Counter : MonoBehaviour
{
    public Coin_Collector coinCollector;
    public Text counterText;
    // Update is called once per frame
    void Update()
    {
          // counterText.text = coinCount.ToString("0");
    }
}
